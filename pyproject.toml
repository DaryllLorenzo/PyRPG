[project]
name = "pyrpg"
version = "0.1.0"
description = "Turn-based JRPG with Pygame UI"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "pygame>=2.5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
